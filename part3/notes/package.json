{
  "name": "notes",
  "version": "1.0.0",
  "description": "notes app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "cd ../../part2/app && rm -rf dist && npm run build && cp -r dist ../../part3/notes && cd ../../part3/notes",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Vastille Dolcine",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  }
}
